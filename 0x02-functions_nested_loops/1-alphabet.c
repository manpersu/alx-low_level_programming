#include "main.h"
/**
*print_alphabet - prints alphabets in lower case
*
*Returns: returns 0 on success
**/
void print_alphabet(void)
{	char a;
	for (a = 'a'; a <= 'z'; a++)
	{
		_putchar(a);
	}
	_putchar('\n');

}
